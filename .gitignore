# Tooling locale — non fa parte del sito pubblicato
.claude/
_bmad/

# macOS
.DS_Store

# Template di lavoro — non pubblicati
docs/templates/
_bmad-output/

# Regia d'aula — non va nel repo pubblico (destinazione: repo privato)
regia/
*.docente.html
